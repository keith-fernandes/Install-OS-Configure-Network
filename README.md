#### Install-OS-Configure-Network

The project is to install a Server Operating System into 2 virtual machines using the Server 2016 ISO disk image. Once the OS is installed the servers needs to be renamed.
